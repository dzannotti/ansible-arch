---
# Modern CLI alternatives and utilities
- name: Install modern CLI alternatives
  become: true
  community.general.pacman:
    name:
      - eza        # Better ls
      - bat        # Better cat
      - fd         # Better find
      - fzf        # Fuzzy finder
      - ripgrep    # Better grep
    state: present
  changed_when: false