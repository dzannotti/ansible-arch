---
- name: Install base packages
  become: true
  community.general.pacman:
    name:
      - base
      - base-devel
      - linux
      - linux-firmware
      - sudo
      - dhcpcd
      - netctl
      - networkmanager
      - openssh
      - nano
      - pipewire
      - man-db
      - man-pages
      - git
      - wget
      - reflector
    state: present
  changed_when: false
